package com.example.rxjava2operator

import android.annotation.SuppressLint

import java.util.ArrayList
import java.util.concurrent.Callable

import io.reactivex.Observable
import io.reactivex.functions.BiConsumer
import io.reactivex.functions.Consumer

/**
 * Created by zhaoyuanchao on 2019-10-18.
 * 将数据收集到数据结构当中。
 */
class CollectExa @SuppressLint("CheckResult")
constructor() {
    init {
//        Observable.just(1, 2, 3, 4)
//            .collect(Callable { ArrayList() },
//                BiConsumer<ArrayList<Int>, Int> { integers, integer -> integers.add(integer) })
//            .subscribe(Consumer {
//
//            })


    }
}
